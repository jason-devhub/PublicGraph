# Route de démonstration front : uniquement en environnement dev (pas en prod).
test_frontend:
    resource: ../../../src/Controller/TestFrontendController.php
    type: attribute
    prefix: '/{_locale}'
    defaults:
        _locale: '%app.default_locale%'
    requirements:
        _locale: '[a-z]{2}'
