framework:
    default_locale: '%app.default_locale%'
    # SYNC obligatoire avec APP_ENABLED_LOCALES (.env) — même membres et ordre (voir docs/upgrade_to_i18n.md).
    enabled_locales: ['en', 'fr']
    translator:
        default_path: '%kernel.project_dir%/translations'
        providers:
