{% extends 'base.html.twig' %}

{% block title %}{{ page_title }} — PublicGraph{% endblock %}

{% block body %}
    <header class="border-b border-rule-strong pb-6">
        <h1 class="font-serif text-3xl font-medium tracking-tight text-text-primary md:text-4xl">{{ page_title }}</h1>
        <p class="mt-2 max-w-2xl font-sans text-sm leading-relaxed text-text-secondary">Structures, partis, réseaux et institutions référencés avec sources.</p>
    </header>
    <div class="mt-10">
        <twig:CatalogOrganizationList />
    </div>
{% endblock %}
