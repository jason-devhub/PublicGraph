{% extends 'base.html.twig' %}
{% import 'catalog/_macros.html.twig' as catalog %}

{% block title %}{{ page_title }} — PublicGraph{% endblock %}

{% block body %}
    <header class="border-b border-rule-strong pb-6">
        <h1 class="font-serif text-3xl font-medium tracking-tight text-text-primary md:text-4xl">{{ page_title }}</h1>
        <p class="mt-2 max-w-2xl font-sans text-sm leading-relaxed text-text-secondary">Acteurs d’influence publiés après modération. Sources obligatoires sur les liens factuels.</p>
    </header>

    <div class="mt-10">
        <twig:CatalogPersonFilters />
    </div>
{% endblock %}
