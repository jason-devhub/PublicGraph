{% extends 'base.html.twig' %}

{% block title %}{{ page_title }} — PublicGraph{% endblock %}

{% block body %}
    <article class="max-w-2xl">
        <h1 class="font-serif text-2xl text-text-primary">{{ page_title }}</h1>
        <p class="mt-4 font-sans text-text-secondary">{{ description|default('Cette page est en cours de rédaction.') }}</p>
    </article>
{% endblock %}
